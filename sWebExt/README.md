
Workflow
-----------------------------------------------
most simple python-web server on local-server-machine,
web-extension will take the ip of said server an send url 
as param in HTTP-request to server to start the scripts via "subprocess".
