**venv/**
example.log
__pycache__/
*.log
*.mp3
*.jpg
*Zone.Identifier*
ffmpeg.exe

# Local config (contains paths + credentials). Distribute config.example.json.
config.json

# Follow-artist runtime files (generated at runtime, not checked in).
follows.json
follow_state.json

# User playlist exports
playlists/*.csv

# Windows build artifacts
windows/Output/
windows/build/
build/
dist/
*.pyc
.cache/
